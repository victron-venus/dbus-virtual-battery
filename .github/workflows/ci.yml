name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    name: CI
    uses: victron-venus/venus-os-ci-toolkit/.github/workflows/python-ci.yml@4d14da8bcf4a827c74ef43ee8339ee174d92caf6
    with:
      python-version: '3.11'
      run-type-check: false  # Disable type checking as the original didn't have it